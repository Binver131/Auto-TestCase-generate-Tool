<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <!-- 引入外部配置文件 -->
    <properties resource="\\mybatis\\jdbc.properties"></properties>

    <settings>
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>
    </settings>
    <!--别名-->
    <typeAliases>
        <!--<typeAlias type="com.zy.mybatis.beans.Student" alias="Student"></typeAlias>-->
        <!--<package name="bean"></package>  -->
        <typeAlias type="bean.Model" alias="Model"></typeAlias>
        <typeAlias type="bean.RowRequirement" alias="RowRequirement"></typeAlias>
        <typeAlias type="bean.Requirement" alias="Requirement"></typeAlias>
        <typeAlias type="bean.Testcase" alias="Testcase"></typeAlias>
        <typeAlias type="bean.Type" alias="Type"></typeAlias>
        <typeAlias type="bean.Variable" alias="Variable"></typeAlias>
        
    </typeAliases>


    <!-- 配置mybatis运行环境 -->
    <environments default="zybatis">
        <environment id="zybatis">
            <!-- type="JDBC" 代表使用JDBC的提交和回滚来管理事务 -->
            <transactionManager type="JDBC" />

            <!-- mybatis提供了3种数据源类型，分别是：POOLED,UNPOOLED,JNDI -->
            <!-- POOLED 表示支持JDBC数据源连接池 -->
            <!-- UNPOOLED 表示不支持数据源连接池 -->
            <!-- JNDI 表示支持外部数据源连接池 -->
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}" />
                <property name="url" value="${jdbc.url}" />
                <property name="username" value="${jdbc.username}" />
                <property name="password" value="${jdbc.password}" />
            </dataSource>
        </environment>
    </environments>
    <!--mybatis.cfg.xml里注册UserMapper.xml文件-->
    <mappers>
        <!-- <package name="mapper"></package> -->
        <mapper resource="mapper/ModelMapper.xml"/>
        <mapper resource="mapper/RowRequirementMapper.xml"/>
        <mapper resource="mapper/RequirementMapper.xml"/>
        <mapper resource="mapper/TestcaseMapper.xml"/>
        <mapper resource="mapper/TypeMapper.xml"/>
        <mapper resource="mapper/VariableMapper.xml"/>

        
        <!--&lt;!&ndash; 告知映射文件方式1，一个一个的配置-->
       <!-- <mapper resource="com/zy/mybatis/mapper/UserMapper.xml"/>
        <mapper resource="com/zy/mybatis/mapper/StudentMapper.xml"/>
        -->
        <!-- 告知映射文件方式2，自动扫描包内的Mapper接口与配置文件 -->
        <!--<package name="com/cy/mybatis/mapper"/>-->
    </mappers>

</configuration>
